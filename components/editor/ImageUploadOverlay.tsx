import type { RefObject } from 'react';
// Stub — image upload not needed for CV context
export function ImageUploadOverlay(_props: { containerRef: RefObject<HTMLDivElement | null> }) { return null; }
